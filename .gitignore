target/
_out/

# The mica-build-env release, fetched and verified at its pin
# (deps/build-env.json) by scripts/build/build-env.sh; never edited in place,
# never committed here.
/build-env/

# Repository-local scratch the packaging driver bind-mounts into containers,
# and the agent sandbox's project-scoped throwaway directory.
tmp/
.tmp/

# Retained, never committed and never cleaned with generated output: material
# kept for provenance, such as the retired mica-deploy repository's refs,
# bundles and release assets under .retained/mica-deploy/<date>/.
/.retained/
