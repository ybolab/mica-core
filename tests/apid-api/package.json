{
  "name": "apid-api-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Black-box HTTP suite for mos apid, run against a booted QEMU guest.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "selftest": "bun run src/selftest.ts",
    "spec-pins": "bun run src/spec-pins.ts",
    "suite": "bun run src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
