[package]
name = "micad-settings"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# The audit ring stamps every line with an RFC 3339 UTC timestamp; both
# binaries already link chrono, so the shared writer adds no crate to an image.
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tempfile = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
url = { workspace = true }
